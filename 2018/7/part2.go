package main

import (
	"aoccommon"
	"fmt"
)

func compute(lines []string, vertexCount int) int {
	return 42
}

func part2() {
	lines, err := aoccommon.ReadLines("input")
	aoccommon.CheckError(err)
	fmt.Printf("part2; lines: %d\n", len(lines))
}
